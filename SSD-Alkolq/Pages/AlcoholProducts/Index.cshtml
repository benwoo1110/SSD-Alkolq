@page
@model SSD_Alkolq.Pages.AlcoholProduct.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AlchoholProduct[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AlchoholProduct[0].Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AlchoholProduct[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AlchoholProduct[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AlchoholProduct[0].ListDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AlchoholProduct) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ListDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
