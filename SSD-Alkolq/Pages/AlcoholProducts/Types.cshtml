@page "{typeName}"
@model SSD_Alkolq.Pages.AlcoholProducts.TypesModel

@{
    ViewData["Title"] = Model.TypeName;
}

<link rel="stylesheet" href="~/css/alcohol_type.css">

<section id="main">
    <h1 class="type-title">@Model.ProductType.Name</h1>
    <img src="~/images/@Model.ProductType.ImageName" alt="beer" class="type-image" />
    <p class="type-description">@Model.ProductType.Description</p>
</section>

<div>
    @foreach (var item in Model.AlcoholProducts)
    {
        <div class="type-card">
            <a class="type-alcoholinfo" asp-page="/AlcoholProducts/Details" asp-route-id="@item.ID">
                <img src="~/images/@item.ImageName" , class="type-alcoholimg" />
                <p class="type-alcoholname">
                    @Html.DisplayFor(modelItem => item.Name)
                </p>
                <p class="type-alcoholprice">
                    @Html.DisplayFor(modelItem => item.Price)
                </p>
            </a>
            @if (Model.IsItemInCart(item))
            {
                <button class="btn btn-secondary disabled">Already in Cart</button>
            }
            else
            {
                <form asp-page-handler="AddToCart" method="post">
                    <input type="hidden" value="@item.ID" name="id" />
                    <button class="btn btn-primary">Add to Cart</button>
                </form>
            }
        </div>
    }
</div>